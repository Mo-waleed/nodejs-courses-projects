// import jwt from "jsonwebtoken"
// export const  generateJWT = async (payload: string | object) => {
//     const token = jwt.sign(payload,process.env.JWT_SEKRET_KEY,{expiresIn:"60m"},{ algorithm: "none" });
//     return token;
// }